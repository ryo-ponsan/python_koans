# Koans 学習メモ（各ファイル 2 行）

> 使い方: 緑化のたびに以下テンプレで 2 行だけ追記。長文禁止、後から読み返せる粒度。

---

## テンプレ

```text
### {YYYY-MM-DD} {FILE_NAME}
- ポイント: {何が分かったか 1 行}
- つまずき/境界: {どこで詰まったか・注意点 1 行}
```

---

## 記録

### 2025-09-01 about_asserts.py（例）

- ~~ポイント: テスト駆動の型（Red→Green→Refactor）、プロンプトもコードも“テストしやすい粒度”に分解の重要性~~
- つまずき/境界: pytest の assert と unittest の assert は別物。あまり pytest で得られる知識
### 2025-09-013 about_none.py

- ~~ポイント: catch attiribute error
- つまずき/境界: attribute error の理解。クラスの復習しないと

### 2025-09-18 koans/about_true_and_false.py

- falseになる値、リスト、タプル、辞書、集合を思い出した
- nvimの入力にもっと慣れよう

### 2025-09-20 koans/about_strings.py`

- """ """で囲むとすべて文字列扱い
- "" や '' の場合、/ をいれると直後の文字列は特殊文字でも文字扱い
- 改行は一文字

# 2025-09-20 koans/about_lists.py`
- スライスで、逆順にもできるし、条件をある程度絞って（偶数、奇数、何番目以降
抽出はわりとできてしまう。
- 開始点、終了点、ステップでの指定で終了点は含まれない。終了点のインデックスがわかればrangeでの範囲を計算で簡単にわかる4-1 のような。含まれると＋１しないとでないというややこしさのみが理由とのこと。

# 2025-09-28 koans/about_lists_assignments.py`
- *first_names は最初と最後の要素の間に何もないため、空のリスト
  `[]` を受け取ります　といった使い方知らなかった。
- * を使ったアンパッキング（通称「スター付き代入」や「拡張アンパッキング
  」）が役立つ具体的なシーンを
- 5つの要素を持つリスト
  data = [10, 20, 30, 40, 50]
    - 最初と最後だけを取り出し、中間は無視する
  first, *_, last = data
  print(f"First: {first}")  # -> First: 10
  print(f"Last: {last}")    # -> Last: 50
  # * には [20, 30, 40] が入るが、変数名が
  なので「意図的に使わない」ことを示せる
  def find_errors_in_logs(*log_files):
      """
      引数で渡された全てのログファイルを解析し、'ERROR'を含む行を出力する
      """
      print(f"--- {len(log_files)}個のファイルを解析します ---")
      for filepath in log_files:

# 2025-09-28 about_tuples.py

- タプルは編集不可
   1. タプルは「不変（Immutable）」である
   2. 様々な作り方がある
   3. 「変更」するには、新しいタプルを作り直す
   4. データ構造として便利
   決まったデータを安全に扱える、軽量な読み取り専用のデータレコード

